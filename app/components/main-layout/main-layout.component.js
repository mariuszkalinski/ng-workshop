import template from './main-layout.template.html';
import controller from './main-layout.controller';

const MainLayoutComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller,
};

export default MainLayoutComponent;
